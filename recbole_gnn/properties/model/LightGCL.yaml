embedding_size: 64              # (int) The embedding size of users and items.
n_layers: 3                     # (int) The number of layers in LightGCL.
dropout: 0.0                    # (float) The dropout ratio.
temp: 0.2 #0.8                       # (float) The temperature in softmax.
lambda1: 0.2 #1e-7 #0.01                   # (float) The hyperparameter to control the strengths of SSL.
lambda2: 1e-7 #1e-5                  # (float) The L2 regularization weight.
q: 5                            # (int) A slightly overestimated rank of the adjacency matrix.